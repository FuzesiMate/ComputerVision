#pragma once
#include <string>
#include "DataTypes.h"

class MessageSerializer
{
public:
	MessageSerializer()=delete;

	//static std::string serializeToJSON(ModelData input);
	//static cvdata::ObjectPositions serializeToProto(ModelData input);

	~MessageSerializer()=delete;
};

